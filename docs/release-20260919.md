# 2026-09-19 交易所整合发布

## 合并范围

以 `C:\workspace\fx\705` 为权威源码，整合此前未提交的权限、会话、资金并发、输入校验、限价撮合、行情隔离、K 线、窄屏布局、盈亏分享及相关测试和素材。

本次同时修复客户端指定成交价的问题：合约市价开仓、手动/管理平仓、止盈止损结算和期权开仓使用服务端新鲜行情，允许后台设置的价格偏移。客户端 `currentPrice`、`closePrice` 不再决定上述成交价格。行情不可用或过期时拒绝成交；限价单等待新鲜行情撮合。保证金、手续费、杠杆和利润公式沿用原规则。

其他 worktree 没有独立的未合并功能代码。`codex/restore-2024-integration` 仅包含另一项恢复任务的规划文档，保留在原分支，不混入本次运行版本。

## 验证

- Java 8 后端构建通过。
- 独立 MySQL 5.7 / Redis 环境完整后端测试：36 项，失败 0、错误 0、跳过 0。包括实际数据库资金断言、权限与会话、限价撮合竞争、价格篡改及行情故障隔离。
- PC、移动端、管理端均通过 TypeScript 检查和生产构建。
- 两端图表数据测试共 8 项通过；盈亏分享的结算、收益率、隐私、历史覆盖和绘制断言通过。
- 外部行情和邮件的可用性不由模拟测试证明。本次不进行真实支付、提现或邮件投递。

验证日志保存在本机 `C:\workspace\fx\releases\705-20260919`；测试数据库与线上隔离。

## 线上发布与迁移

现有项目为服务器上的 `fx-705-prod`，公网 PC 入口为 `https://fx.pennyshop.bz/`。本次产物目录为 `/data/pennymall/fx-705/release-20260919`，保留原 MySQL、Redis 数据卷及 `/data/pennymall/fx-705/release-20260911/uploads`。

发布前停止后端写入，备份数据库和上传文件。根据现有字段和索引执行以下两个脚本中缺失的增量变更，不重复执行已存在的字段：

- `exchange-backend/src/main/resources/db/manual/705-minimal-fix.sql`
- `exchange-backend/src/main/resources/db/manual/705-limit-matching.sql`

新增乐观锁版本字段、请求幂等标识、账户唯一索引及会话/验证码字段。历史订单的 `limit_match_enabled` 保持 `0`，不自动补成交，不重算历史利润或余额。迁移前后核对账户与订单数量及资金合计。

发布后检查三端 HTTP、API、WebSocket、公网入口及产物摘要。旧登录会话可能需要重新登录。

健康检查使用公开的 `/api/user/system/timezone`，避免继续请求已限制为超管访问的图片诊断接口。

旧后端镜像保留为 `fx-705-backend:rollback-20260919`，旧静态文件保留在原发布目录。新目录中的 `rollback.yaml` 配合旧 `compose.prod.yaml` 可回退应用版本；不得为回退应用自动恢复旧数据库，避免覆盖发布后的业务数据。

密码、令牌、`.env`、私钥、数据库备份和运行数据不提交 Git。
